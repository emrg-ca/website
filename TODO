continue to get source documents for existing pdf versions
convert broken pptx and docx files to odp and odt
script to regenerate all html headers
clean up directory structure some more
standardize styles and left menubar stuff
import malformed dmap documents
import cn cycle audio and documents
import more of mike's tutorial slides
fix emrg dns records for www and to backup peter
fix/update all emrg email aliases and to backup peter
allow emrg host access via ssh, git, rsync to backup metadata
actually push a clone of the git repo somewhere else safe
digitally sign documents that get autogenerated
